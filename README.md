# БУРМАШ · CRM Дашборд v5.7

**Продвинутая CRM-аналитика на основе Bitrix24** с поддержкой AI, детекцией обхода системы и полным анализом воронок продаж.

![Version](https://img.shields.io/badge/version-5.7-blue)
![License](https://img.shields.io/badge/license-MIT-green)
![Python](https://img.shields.io/badge/python-3.9+-blue)

## ✨ Основные возможности

- **📊 Гибкая система фильтров**: НИТ, Год, Квартал, Месяц, Неделя, Диапазон дат
- **📈 Динамика по периодам**: Автоматическое сравнение текущего с предыдущим периодом
- **💰 Расчет выручки**: Правильная агрегация из 3 воронок продаж с соответствующими стадиями успеха
- **🔄 Анализ провалов**: Детальное разбиение по 2 группам причин (ранние и поздние этапы)
- **📅 Градация сделок**: Quick Wins, Проработка, Stop List
- **🤖 AI-анализ**: Рекомендации по здоровью сделок (опционально)
- **🚨 Античит**: Детекция попыток обхода системы (переносы дедлайнов, микро-задачи)
- **👥 Фильтры по отделам**: Подсчет по отделам и сотрудникам

## 📋 Требования

- Python 3.9+
- Bitrix24 REST API Webhook (обязательно)
- Perplexity API (опционально для AI-анализа)

## 🚀 Быстрый старт

### Локально

```bash
# 1. Клонировать репозиторий
git clone https://github.com/your-username/burmash-crm-dashboard.git
cd burmash-crm-dashboard

# 2. Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Подготовить конфиг
mkdir -p .streamlit
cp secrets.example.toml .streamlit/secrets.toml

# 5. Заполнить API ключи в .streamlit/secrets.toml
# BITRIX24_WEBHOOK = "https://your-domain.bitrix24.com/rest/1/..."
# PERPLEXITY_API_KEY = "..."  # Опционально

# 6. Запустить
streamlit run dashboard-1.py
```

### На Streamlit Cloud

1. Форкни репозиторий на GitHub
2. Перейди на https://share.streamlit.io
3. Выбери: New app → ваш репо → `dashboard-1.py`
4. Settings → Secrets → добавь:
   ```toml
   BITRIX24_WEBHOOK = "..."
   PERPLEXITY_API_KEY = "..."  # опционально
   ```

## 📁 Структура проекта

```
burmash-crm-dashboard/
├── dashboard-1.py              # Основной скрипт (v5.7)
├── requirements.txt            # Python зависимости
├── README.md                   # Документация
├── LICENSE                     # MIT License
├── .gitignore                  # Git ignore
├── pyproject.toml              # Конфиг проекта
├── .streamlit/
│   ├── config.toml             # Конфиг Streamlit
│   └── secrets.toml            # API ключи (НЕ коммитить!)
├── .github/
│   └── workflows/
│       └── tests.yml           # GitHub Actions
├── secrets.example.toml        # Шаблон secrets.toml
└── setup.sh / setup.bat        # Скрипты установки
```

## ⚙️ Конфигурация

### .streamlit/secrets.toml

```toml
BITRIX24_WEBHOOK = "https://your-domain.bitrix24.com/rest/1/your-webhook-code"
PERPLEXITY_API_KEY = "your-perplexity-api-key"  # опционально
```

### Переменные окружения (альтернатива)

```bash
export BITRIX24_WEBHOOK="https://..."
export PERPLEXITY_API_KEY="..."
```

## 📊 Разделы дашборда

### 1. Обзор
- Суммарные показатели (сделки, выручка, здоровье, потенциал) с динамикой
- Распределение здоровья (воронка с шагом 5%)
- Анализ воронок с детализацией провалов
- Анализ провалов по 2 группам стадий

### 2. Проблемы
- Метрики проблем: без задач, компании, контакта, застряли, проиграны
- График динамики проблем
- Списки сделок по каждому типу проблемы

### 3. По менеджерам
- Таблица аналитики (выручка, здоровье, конверсия)
- Визуализация (выручка, scatter plot)
- Конверсия по этапам воронки

### 4. Проблемные сделки
- Сделки без задач
- Сделки без компании
- Сделки без контакта
- Застрявшие сделки
- Проигранные сделки

### 5. Обзор потенциала
- Распределение по диапазонам суммы
- Средний потенциал по менеджерам

### 6. Тренды и прогноз (опционально)
- AI-прогноз при наличии API ключей

## 🔐 Безопасность

**ВАЖНО:**
- Никогда не коммитьте `.streamlit/secrets.toml` в Git
- Используйте `secrets.example.toml` как шаблон
- На Streamlit Cloud используйте Settings → Secrets

## 🧠 Фильтры

| Фильтр | Описание |
|--------|---------|
| **НИТ** | Начало исчисления территории (с какой даты отслеживать) |
| **Год** | Фильтр по году |
| **Квартал** | Фильтр по кварталу (выбираем год + квартал) |
| **Месяц** | Фильтр по месяцу (год + месяц) |
| **Неделя** | Фильтр по ISO-неделе (год + неделя) |
| **Диапазон дат** | Произвольный диапазон |
| **Агрегация** | Авто/Дни/Недели/Месяцы для графиков |
| **Нет активности** | Порог дней для статуса "застряла" |
| **Лимит сделок** | Максимум сделок для загрузки из API |
| **Отделы** | Фильтр по отделам/сотрудникам |

## 📈 Метрики

### Здоровье сделки (0-100%)
```
базовое: 100
- нет компании: -10
- нет контакта: -10
- нет открытых задач: -25
- застрявшая (без активности): -25
- проигранная: min(score, 15)
```

### Потенциал сделки (0-100%)
```
основано на:
- сумме (логарифмическая шкала)
- вероятности закрытия
- формула: (30 + log10(сумма)/5*70) * (0.4 + вероятность/100*0.6)
```

### Выручка
Агрегируется из 3 воронок:
- **Основная воронка продаж** → стадия "Успешно реализовано"
- **Физ.Лица** → стадия "Сделка успешна"
- **Не приоритетные сделки** → стадия "Сделка успешна"

### Провалы
**Группа 1** (ранние этапы):
- Недозвон, Не абонент, СПАМ, Нецелевой, Дорого, Организация не действует, Был конфликт, Не одобрили отсрочку, Не устроили сроки, Сделка отменена, Удалено из Авито

**Группа 2** (поздние этапы):
- Выбрали конкурентов, Дорого, Был конфликт, Не одобрили отсрочку, Не устроили сроки, Сделка отменена

## 🚨 Античит-система

Отслеживает подозрительную активность:
- **Переносы дедлайнов** - количество уникальных дней DEADLINE для одной задачи (≥3 = подозрительно)
- **Микро-задачи** - задачи продолжительностью ≤15 минут (≥5 = подозрительно)

Флаг `cheat_flag = TRUE` если:
- reschedules ≥ 3 ИЛИ
- micro_tasks ≥ 5

## 🔗 API Bitrix24

Используемые методы:
- `crm.deal.list` - получение сделок
- `crm.dealcategory.list` - получение воронок
- `crm.dealcategory.stage.list` - получение стадий
- `crm.status.list` - альтернативный способ получения стадий
- `crm.activity.list` - получение активностей (задач)
- `user.get` - получение пользователей
- `department.get` - получение отделов
- `crm.stagehistory.list` - история стадий

## 🤖 AI Integration (опционально)

При наличии `PERPLEXITY_API_KEY`:
- Рекомендации по здоровью сделок
- Анализ по менеджерам
- Тренды и прогнозы

Без ключа - база функционала работает полностью.

## 📖 Документация

- Основные настройки в коде (константы в начале `dashboard-1.py`)
- Примеры использования API см. в функциях `bx_*`

## 🧪 Тестирование

```bash
# Локально с debug логами
streamlit run dashboard-1.py --logger.level=debug

# Проверка синтаксиса
python -m py_compile dashboard-1.py
```

## 🐛 Troubleshooting

| Проблема | Решение |
|----------|---------|
| "BITRIX24_WEBHOOK не указан" | Добавьте в `.streamlit/secrets.toml` |
| Графики не отображаются | `pip install --upgrade plotly` |
| Медленная загрузка | Уменьшите "Лимит сделок" в фильтре |
| Ошибка типов в скоринге | Используйте последнюю версию (v5.7) |
| Истории стадий не загружаются | Отключите "Использовать историю стадий" |

## 📝 Лицензия

MIT License - см. `LICENSE`

## 🤝 Вклад

Приветствуются pull requests и issues!

## 📞 Поддержка

При проблемах:
1. Проверьте логи: `streamlit run dashboard-1.py --logger.level=debug`
2. Убедитесь в доступности Bitrix24 API
3. Откройте Issue на GitHub

## 🔄 История версий

### v5.7 (текущая)
- ✅ Фикс TypeError в cheat_flags_for_deal (разные TZ/типы)
- ✅ Устойчивый расчет потенциала (NaN/inf)
- ✅ Фильтры с сохранением в session_state
- ✅ Анализ провалов по 2 группам
- ✅ Фильтры по отделам

### v5.0+
- Динамика по периодам
- Правильный расчет выручки из 3 воронок
- AI-аналитика

### v4.x
- Базовый функционал

---

**Создано для БУРМАШ** 🟧  
Made with ❤️ using Streamlit + Bitrix24 + Perplexity AI
